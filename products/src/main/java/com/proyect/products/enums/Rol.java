package com.proyect.products.enums;

/**
 * Enum que define los roles permitidos dentro del sistema.
 * 
 * Se utiliza para evitar errores al escribir los roles
 * manualmente como texto y garantizar que solo existan
 * valores válidos en la aplicación.
 */
public enum Rol {
    ADMIN,
    CASHIER
}
